//Writing a  program to find the closest value to 100 from two numerical values

const firstNumber = parseInt(prompt("Enter first number : "));
const secondNumber = parseInt(prompt("Enter second number : "));

if (firstNumber && secondNumber >= 95) {
  console.log(`${firstNumber} is closed to 100`);
}else if(){

}
 else {
  console.log(`${secondNumber} is closed to 100`);
}
